import React from "react";

const reducer = (state, action) => {
  switch (action.type) {
    //   case  harchi:
    //       return {...state, }

    default:
      return state;
  }
};

export default reducer;
